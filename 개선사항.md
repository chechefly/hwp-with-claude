# 개선사항 (실전 발견 버그 · 해결 기록)

실사용 세션에서 발견된 버그와 해결 내역. 최신이 위.

## v0.8.0 (2026-08-31)

### 🔴🔴🔴🔴 체크박스가 기존 네모를 '교체' 안 하고 '삽입'만 함 — 해결
- **증상**: `hwp_check_by_label`이 라벨 뒤 ☐/□를 지우지 않고 ☑를 덧붙여 `☐☑`(이중박스)가 됨.
- **원인**: ☐(U+2610) 경로가 `op_check_after`(커서 이동 후 삽입)로 처리됐는데, 박스 위치(공백 수)가
  폼마다 달라 선택이 빗나가면 교체가 아니라 삽입이 됐음.
- **해결**: ☐·□ 모두 `AllReplace`로 **기존 박스 문자를 mark로 직접 교체**(위치 안 탐, 앞/뒤·공백 0~2칸).
  `[ ]`(ASCII 대괄호)도 `[√]`로 교체. 못 찾으면 **억지 삽입 없이 `박스 못 찾음(수동 체크 필요)`로 정직 보고**.
  (이중박스 유발하던 삽입 폴백 제거)

### 🔴🔴🔴 삽입 값이 검정·기본서식으로 강제되지 않음 — 해결
- **증상**: 값이 칸의 예시서식(회색·굵게 등)을 물려받아 회색으로 들어감.
- **해결**: `hwp_fill_by_label`에 `text_style` 추가(**기본값 `reset`**). 삽입 값을 **검정·강조해제·기본 글자모양**으로
  강제. `keep`이면 칸 서식 유지.

### 🔴🔴🔴 저장이 실패하던 진짜 원인 = 읽기전용 파일 — 해결
- **증상**: 일부 .hwp에서 `hwp_save`가 "저장 실패". (렌더 후라서 그런 줄 알았으나 오진)
- **원인**: 다운로드한 관공서 .hwp가 **읽기전용(read-only)** 속성인 경우가 흔한데, HWP가 읽기전용으로 열면
  그 모드로 고정돼 이후 SaveAs(같은 경로)가 조용히 실패. (속성을 나중에 풀어도 안 됨 — 열 때 이미 고정)
- **해결**: `hwp_open`에서 **열기 직전에 read-only 해제**. 이후 저장 정상.

## v0.7.0 (2026-08-31)

### 표 칸 이동이 안 되던 관공서 서식 — 해결
- **증상**: 융자신청서 등에서 라벨 옆 값 칸으로 이동 실패("인접 셀 없음"), 값이 라벨 셀에 박힘.
- **원인**: 해당 표는 셀 안인데도 `TableRightCell`이 작동하지 않음.
- **해결**: `TableRightCell` 실패 시 **커서를 문자단위(MoveRight)로 밀어 셀 경계를 넘어** 값 칸 진입.
  삽입 후 **문서 텍스트로 실제 반영 검증**(가짜 성공 제거). 파괴 방지 가드(셀 진입 확인 후에만 삭제).
