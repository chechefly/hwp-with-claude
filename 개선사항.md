# 개선사항 (실전 발견 버그 · 해결 기록)

실사용 세션에서 발견된 버그와 해결 내역. 최신이 위.

## v1.3.0 (2026-08-31) — 실전 4종 서식 완주로 발견한 5건 개선
실제 강남구 육성기금 서식(사업계획서·개인정보동의서·무상거주확인서·특별신보신청서)을 처음부터 끝까지
작성하며 발견한 문제들. 전부 렌더로 검증.
- **글자 겹침(뭉개짐) 근본 해결**: 줄배치 캐시(linesegarray)가 남아 긴 텍스트가 옛 줄 폭에 눌리던 문제
  → set_text·replace_text 모두 캐시 제거, 한/글이 재계산 → 긴 서술도 자연 줄바꿈.
- **글꼴까지 리셋**: 필기체 칸에 값이 손글씨처럼 들어가던 문제 → reset이 색·굵기에 더해
  글꼴(fontRef·장평·자간)을 문서 기본(charPr id=0)으로 교체. 크기는 칸 비례 유지.
- **hwp_replace에 count**: '20 년 월 일' 치환이 작성예시까지 오염시키던 문제 → count=1로 빈 양식만.
- **hwp_check_by_label에 nth·table**: '□기타'가 여러 그룹에 있을 때 N번째만 체크.
- **확장자 위장 파일 대응**: '.hwpx'인데 실제는 구형 .hwp(CFB)인 관공서 파일 실존
  → 확장자 대신 파일 매직(PK/CFB)으로 판별해 자동 변환 경로로.

## v1.2.0 (2026-08-31) — 레이아웃 지능 1차
- **이미지 삽입** `hwp_insert_image`: PNG/JPG를 셀 안에 '글자처럼' 삽입(서명·도장). `after_text`로
  "서명 또는 날인" 같은 특정 줄에 정확히 배치. BinData+매니페스트+hp:pic 전부 XML로 구성.
- **행 추가/삭제** `hwp_table_rows(add/delete)`: 기준 행을 서식 템플릿으로 복제(높이·괘선·글자모양 승계)
  → 표 모양 통일. 세로병합 관통은 자동 확장/축소, 병합 시작 행 삭제는 정직하게 거부.
- **페이지 맞춤**: `hwp_layout_report`(페이지별 내용 하단 % 리포트) + `hwp_table_rows(scale_height)`
  (행 최소높이 압축/확장) → 리포트→조절→렌더 루프로 넘침/빈 페이지 정리. 행 추가 후에도 압축으로
  1페이지 유지되는 것 렌더 검증.

## v1.1.0 (2026-08-31)
- **여러 줄 값 지원**: `\n`이 있으면 줄마다 문단 생성(주소 2줄 등). 어색한 줄바꿈은 셀 통째 재작성으로 정리.
- **★서식 강제(reset)**: 폼 칸에 걸린 이상한 색/굵기(회색 안내문·빨강 강조 등)를 **안 물려받음** —
  삽입값용 '정리된 글자모양'(검정·바탕없음·강조해제)을 header에 생성해 참조. 글꼴·크기는 폼 정의 유지.
  기본 `text_style="reset"`, 칸 서식을 따르려면 `"keep"`. (빨강+굵게 오염 칸으로 렌더 검증)

## v1.0.0 (2026-08-31) — ★엔진 교체: 편집은 XML 직접 편집으로 통일

- **구조 전환**: HWPX(zip+XML)를 직접 파싱·수정하는 `hwpx_engine.py` 신설.
  편집(채우기·체크·치환·표읽기)은 **전부 XML**, 한/글(COM)은 **hwp↔hwpx 변환·PDF 렌더 전용**으로 강등.
- **효과**: 표 구조(셀 주소·병합·내용)가 XML에 명시돼 있어 **"이 폼은 되나?"가 소멸**.
  커서·Find·클립보드·표이동 등 v0.x 버그의 발원지였던 COM 편집 조작 전면 폐기.
- `.hwp`는 열 때 같은 폴더에 `.hwpx` 작업본 자동 생성(1회 변환), `hwp_save` 시 `.hwp` 자동 반영.
- `fill_by_label`에 `table`(N번째 표만) 추가 — **작성예시 표가 있는 문서에서 예시를 안 건드림**.
- `check_by_label`: '□ A □ B' 그룹에서 다음 항목 박스를 훔치는 오염 방지(셀 구조 판별),
  '이미 체크됨' 구분 보고 추가.
- `table_map`/`set_cells` 주소 체계: `r행c열`(정확 주소). 결과 `OK(t0 r1c4)`=기입 좌표 명시.
- 검증: 융자신청서(.hwp, 값 10+체크 3종+예시 보존), 신용보증신청서(자간공백 라벨), 체크리스트(.hwpx) 렌더 확인.

## v0.8.0 (2026-08-31)

### 🔴🔴🔴🔴 체크박스가 기존 네모를 '교체' 안 하고 '삽입'만 함 — 해결
- **증상**: `hwp_check_by_label`이 라벨 뒤 ☐/□를 지우지 않고 ☑를 덧붙여 `☐☑`(이중박스)가 됨.
- **원인**: ☐(U+2610) 경로가 `op_check_after`(커서 이동 후 삽입)로 처리됐는데, 박스 위치(공백 수)가
  폼마다 달라 선택이 빗나가면 교체가 아니라 삽입이 됐음.
- **해결**: ☐·□ 모두 `AllReplace`로 **기존 박스 문자를 mark로 직접 교체**(위치 안 탐, 앞/뒤·공백 0~2칸).
  `[ ]`(ASCII 대괄호)도 `[√]`로 교체. 못 찾으면 **억지 삽입 없이 `박스 못 찾음(수동 체크 필요)`로 정직 보고**.
  (이중박스 유발하던 삽입 폴백 제거)

### 🔴🔴🔴 삽입 값이 검정·기본서식으로 강제되지 않음 — 해결
- **증상**: 값이 칸의 예시서식(회색·굵게 등)을 물려받아 회색으로 들어감.
- **해결**: `hwp_fill_by_label`에 `text_style` 추가(**기본값 `reset`**). 삽입 값을 **검정·강조해제·기본 글자모양**으로
  강제. `keep`이면 칸 서식 유지.

### 🔴🔴🔴 저장이 실패하던 진짜 원인 = 읽기전용 파일 — 해결
- **증상**: 일부 .hwp에서 `hwp_save`가 "저장 실패". (렌더 후라서 그런 줄 알았으나 오진)
- **원인**: 다운로드한 관공서 .hwp가 **읽기전용(read-only)** 속성인 경우가 흔한데, HWP가 읽기전용으로 열면
  그 모드로 고정돼 이후 SaveAs(같은 경로)가 조용히 실패. (속성을 나중에 풀어도 안 됨 — 열 때 이미 고정)
- **해결**: `hwp_open`에서 **열기 직전에 read-only 해제**. 이후 저장 정상.

## v0.7.0 (2026-08-31)

### 표 칸 이동이 안 되던 관공서 서식 — 해결
- **증상**: 융자신청서 등에서 라벨 옆 값 칸으로 이동 실패("인접 셀 없음"), 값이 라벨 셀에 박힘.
- **원인**: 해당 표는 셀 안인데도 `TableRightCell`이 작동하지 않음.
- **해결**: `TableRightCell` 실패 시 **커서를 문자단위(MoveRight)로 밀어 셀 경계를 넘어** 값 칸 진입.
  삽입 후 **문서 텍스트로 실제 반영 검증**(가짜 성공 제거). 파괴 방지 가드(셀 진입 확인 후에만 삭제).

