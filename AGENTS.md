# AGENTS.md — AI 어시스턴트(Claude Code 등)를 위한 안내

사용자가 이 저장소를 가져오면(URL 붙여넣기·클론·다운로드 등), 아래 **온보딩 대화 흐름**을 따라 사용자를 안내하세요. 기계적으로 설치만 하지 말고, **먼저 이게 뭔지 설명하고 → 동의받고 → 설치 → 첫 사용까지** 이끌어 주세요. 사용자의 언어(한국어면 한국어)로 말하세요.

---

## 1단계 — 먼저 "이게 뭔지" 소개 (설치 전에!)
사용자에게 친근하게 설명하세요. 예:

> "이건 **hwp with claude**예요 — 제가(Claude가) **한글(.hwp) 문서를 대신 작성·편집**할 수 있게 해주는 도구입니다.
> 설치하면 이런 걸 저한테 **말로** 시킬 수 있어요:
> • 신청서·지원서·양식 **자동 작성** (표 칸 채우기)
> • **체크박스 체크**, 항목 선택
> • 금액·표 정리하고 **PDF로 저장**
> • 채운 결과를 **이미지로 보여주기**(확인용)
> 원본은 자동 백업돼서 언제든 되돌릴 수 있어요. 설치해드릴까요?"

## 2단계 — 준비물 확인
- **Windows**인지, **한/글(HWP)이 설치**돼 있는지 확인하세요. (한/글이 없으면 이 도구는 작동하지 않음 — 먼저 한/글 설치 필요)
- 파이썬은 없어도 됩니다(설치기가 자동 설치).

## 3단계 — 설치 (동의받은 뒤)
- 이 폴더에서 **`install.bat`** 실행 (또는 `install.ps1`).
  - 파이썬 자동설치 + 의존성 + Claude 설정 등록 + 스킬까지 자동.
- 결과는 `install_result.txt`에도 저장됨.

## 4단계 — ★재시작 안내 (반드시!)
설치만으론 도구가 안 뜹니다. 사용자에게 꼭 안내하세요:
1. **"Claude를 완전히 종료했다가 다시 켜세요."**
   - Claude Code: 재시작/새 세션
   - Claude 데스크톱: 트레이 아이콘 우클릭 → 종료 → 다시 실행 (창만 닫으면 안 됨)
2. 재시작 후 **`hwp_status`**를 호출해 작동 확인.

## 5단계 — 첫 사용 유도
재시작·확인 뒤:
> "이제 준비됐어요! 채우고 싶은 한글 파일 경로를 주시면(예: `C:\...\신청서.hwp`), 뭘 넣을지 알려주시면 바로 채워드릴게요."

첫 파일을 채운 뒤엔 **`hwp_render`로 결과 이미지를 보여주고**, 저장·닫기까지 안내하세요.

---

## 기술 주의
- 서버(`hwp_mcp_server.py`)를 직접 실행하지 마세요. Claude가 MCP로 자동 실행합니다.
- **서버가 도는 PC는 Windows + 한/글 설치 필수.** (서버가 로컬 한/글을 조종하므로)
- **사용 위치는 Claude Code·데스크톱 채팅·Cowork 전부 가능.** Claude 데스크톱이 로컬 MCP를 Cowork(클라우드) 세션까지 자동 브릿지해줍니다 — 별도 HTTP 호스팅/브릿지 불필요. (단, 그 브릿지가 되려면 데스크톱 커넥터 등록이 되어 있어야 하고, 이는 install.bat + 재시작으로 처리됨)
- 데스크톱 등록 시 설정 파일 덮어쓰기를 피하려면 **모든 Claude 종료 후 설치 → 데스크톱만 재실행**이 가장 안전.
- 사용법·문제해결·기능 상세는 `README.md`, 실행 지침은 `skills/hwp-with-claude/SKILL.md` 참고.
