# AGENTS.md — AI 어시스턴트(Claude Code 등)를 위한 설치·안내 지침

이 저장소를 받아 설치를 도와주는 AI 어시스턴트는 아래를 **반드시** 따르세요.

## 이게 뭔가
`hwp with claude` — Claude가 한글(.hwp) 문서를 자동으로 열고·채우고·저장하게 해주는 **로컬 stdio MCP 서버**입니다. Windows + 한/글(HWP) 설치 필요.

## 설치 방법
1. 이 폴더에서 `install.bat` 실행 (또는 `install.ps1` → 파이썬 자동설치 후 `install.py` 실행).
   - 파이썬이 없으면 자동 설치되고, 의존성·Claude 설정 등록·스킬 설치까지 자동 처리됩니다.
2. 설치 결과는 `install_result.txt`에도 기록됩니다.

## ★ 설치 후 — 사용자에게 반드시 안내할 것 (중요)
설치만으로는 도구가 바로 안 뜹니다. **설정을 다시 읽으려면 재시작이 필요**합니다. 사용자에게 이렇게 안내하세요:

1. **"Claude를 완전히 종료했다가 다시 켜세요."**
   - Claude Code: **재시작 또는 새 세션** 시작
   - Claude 데스크톱 앱: **트레이 아이콘 우클릭 → 종료 → 다시 실행** (창만 닫는 것으로는 부족)
2. 재시작 후 **`hwp_status`** 도구를 호출해 정상 작동을 확인시키세요.
3. 그다음부터는 자연어로 사용: *"C:\경로\양식.hwp 열어서 채워줘"*

## 주의
- 이 서버(`hwp_mcp_server.py`)를 직접 실행하지 마세요. Claude가 MCP로 자동 실행합니다.
- 설치 중 Claude 데스크톱이 켜져 있으면 설정 파일이 덮어써질 수 있으니, 데스크톱용으로 쓸 경우 **모든 Claude 종료 후 설치 → 데스크톱만 재실행**이 가장 안전합니다.
- 사용법·문제해결은 `README.md` 참고.
